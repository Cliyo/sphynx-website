import styled from "styled-components";

export const PrivateRouteContainer = styled.div`
    position: relative;
    display: flex;
    width: 100vw;
    height: 100vh;

    padding-left: 320px;
`